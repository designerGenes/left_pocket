# Target directory (Rust build artifacts)
target/

# Node modules and build output for VS Code Extension
vscode-extension/node_modules/
vscode-extension/out/
vscode-extension/*.vsix

# Graphify output directory itself (to prevent circular scanning)
graphify-out/

# Internal task tracker / metadata directories
.beads/
.gemini/
.opencode/

# System / IDE files
.DS_Store
.git/
.vscode/

# Environment / Secret files
.env
*.env

# Documentation and templates (create isolated nodes, not actual code)
# Markdown files are not code - exclude to keep graph focused on implementation
*.md
src/templates/
