{
  "name": "spocket",
  "displayName": "Corner",
  "description": "Real-time sync between VS Code workspace folders and Corner manifests",
  "version": "2.0.1",
  "publisher": "jadennation",
  "repository": {
    "url": "https://github.com/designerGenes/corner"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:corner.openDailyFeature",
    "onCommand:corner.newDailyFeature"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Corner",
      "properties": {
        "corner.binaryPath": {
          "type": "string",
          "default": "corner",
          "description": "Path to the Corner binary"
        },
        "corner.dailyFeatureSubpath": {
          "type": "string",
          "default": "dailies",
          "description": "Subfolder under FEATURES where new daily feature files are created"
        }
      }
    },
    "commands": [
      {
        "command": "corner.openDailyFeature",
        "title": "Corner: Open Daily Feature"
      },
      {
        "command": "corner.newDailyFeature",
        "title": "Corner: New Daily Feature File"
      }
    ],
    "keybindings": [
      {
        "command": "corner.openDailyFeature",
        "key": "ctrl+alt+t",
        "mac": "ctrl+alt+t",
        "when": "workspaceFolderCount > 0"
      },
      {
        "command": "corner.newDailyFeature",
        "key": "ctrl+shift+alt+t",
        "mac": "ctrl+shift+alt+t",
        "when": "workspaceFolderCount > 0 && corner.inTodaysFeature"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "prepackage": "npm run compile",
    "package": "npx @vscode/vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
